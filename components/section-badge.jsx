export default function SectionBadge({children}) {
    return (
        <div className="inline-block rounded-full px-4 py-1 text-sm mb-4 text-primary-foreground bg-orange/80">
            {children}
        </div>
    )
}
